# frozen_string_literal: true

require 'spec_helper'

RSpec.describe AcaEntities::Protocols::Transmittable::Transaction do
  context 'When a valid payload is used' do
    it 'should create a valid Transaction instance'
  end
end
